import 'package:flutter/material.dart';

class ErrorPageView extends StatelessWidget {
  const ErrorPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Hocam hata var'));
  }
}
